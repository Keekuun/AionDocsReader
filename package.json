{
  "name": "aion-docs-reader",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev --port 5273",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "sync": "node scripts/sync-docs.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
